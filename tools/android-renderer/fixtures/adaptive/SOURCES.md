# Adaptive icon fixtures

`background.svg` is local. The foreground and monochrome layers are Material
Symbols fetched by `build.sh` from the same pinned commit as the conformance
corpus (`google/material-design-icons`, Apache-2.0); nothing is vendored:

- foreground: `symbols/web/stars/materialsymbolsoutlined/stars_24px.svg`
- monochrome: `symbols/web/rocket_launch/materialsymbolsoutlined/rocket_launch_24px.svg`
